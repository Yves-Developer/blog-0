/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["blog-cms-14.onrender.com", "res.cloudinary.com"], // Allow images from this domain
  },
};

export default nextConfig;
